/**
 * @name range
 * @param {number} start
 * @param {number} end
 * @returns {array}
 *
 * Usage:
 * @example range(1;3) // "1¶2¶3"
 * @example range(2;-2) // "2¶1¶0¶-1¶-2"
 */

If(start=end;start;start & "¶" &
range(start+If(start<end;1;-1);end)
)
